newman html reporter installation: 
npm install -g newman-reporter-html

newman htmlextra reporter installation: 
npm install -g newman-reporter-htmlextra

newman running command:
newman run Ses_01MyTestCollection.postman_collection.json -g workspace.postman_globals.json -r htmlextra